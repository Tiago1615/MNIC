P = zeros(6);

% A (absorbente)
P(1,1) = 1;

% B
P(2,[1 3]) = [1/2 1/2];

% C
P(3,[2 4 6]) = [1/3 1/3 1/3];

% D
P(4,[1 3 5]) = [1/3 1/3 1/3];

% E
P(5,[4 6]) = [1/2 1/2];

% F (absorbente)
P(6,6) = 1;

disp(P)

order = [1 6 2 3 4 5];
P_can = P(order, order);
disp(P_can)

Q = P_can(3:6, 3:6);   % transitorios
R = P_can(3:6, 1:2);   % hacia absorbentes

